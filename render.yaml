services:
  - type: web
    name: gram-setu-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20.16.0
      - key: MONGODB_URI
        sync: false
